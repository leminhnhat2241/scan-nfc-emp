name: smartcheck_nfc
description: "A new Flutter project."
publish_to: "none"

version: 2.0.0+5

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # NFC
  nfc_manager:
    path: .local_plugins/nfc_manager

  # Database
  sqflite: ^2.3.0
  path: ^1.9.0

  # Utilities
  intl: ^0.20.2
  shared_preferences: ^2.2.2

  # Text-to-Speech
  flutter_tts: ^4.2.0

  # Camera for Anti-Fraud
  camera: ^0.11.0+2
  path_provider: ^2.1.4

  # Google Sheets Integration
  http: ^1.2.2

  # Charts for Analytics
  fl_chart: ^0.69.0
  
  # Biometrics
  local_auth: ^2.1.6
  
  # Email launcher
  url_launcher: ^6.2.1

  # PDF Export (NEW)
  pdf: ^3.10.4
  printing: ^5.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
